#include"formatData.h"

std::string formatData::FormatData(std::string x){
    //Check for double spaces


}

uint8_t formatData::doubleSpaceCheck(std::string *content){
    uint8_t len;
    for(size_t i = 0; i < len;i++){
        //if(isspace(content[i]))
    }
}